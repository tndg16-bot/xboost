# エージェント起動スクリプト - コピペ用

---

## 📋 エージェントAの起動

```javascript
task({
  description: "タスク",
  prompt: "あなたはエージェントAです。\n\nタスク: [ここにタスクを書く]\n\n1時間ごとに進捗を報告してください。",
  subagent_type: "general",
  tools: { "background_task": false }
})
```

---

## 📋 エージェントBの起動

```javascript
task({
  description: "タスク",
  prompt: "あなたはエージェントBです。\n\nタスク: [ここにタスクを書く]\n\n1時間ごとに進捗を報告してください。",
  subagent_type: "general",
  tools: { "background_task": false }
})
```

---

## 📋 エージェントCの起動

```javascript
task({
  description: "タスク",
  prompt: "あなたはエージェントCです。\n\nタスク: [ここにタスクを書く]\n\n1時間ごとに進捗を報告してください。",
  subagent_type: "general",
  tools: { "background_task": false }
})
```

---

## 🚀 3つのエージェントを一気に起動（並列実行）

```javascript
// エージェントA
task({
  description: "タスクA",
  prompt: "あなたはエージェントAです。\n\nタスク: [タスクAの説明]",
  subagent_type: "general",
  tools: { "background_task": false }
})

// エージェントB（並列実行）
task({
  description: "タスクB",
  prompt: "あなたはエージェントBです。\n\nタスク: [タスクBの説明]",
  subagent_type: "general",
  tools: { "background_task": false }
})

// エージェントC（並列実行）
task({
  description: "タスクC",
  prompt: "あなたはエージェントCです。\n\nタスク: [タスクCの説明]",
  subagent_type: "general",
  tools: { "background_task: false }
})
```

---

## 🔍 詳細なタスク指示

```javascript
task({
  description: "TypeScriptエラー修正",
  prompt: `あなたはエージェントCです。TypeScriptエラーの修正を担当します。

## タスク
以下のファイルのTypeScriptエラーを修正してください：
- app/page.tsx
- components/Header.tsx

## 手順
1. ファイルを開く
2. エラーを確認
3. 修正する
4. npm run typecheck で確認

## 進捗報告
1時間ごとに以下のフォーマットで報告してください：

\`\`\`
## 進捗報告
エージェント: C
タスクID: C-1
アクション: 進捗更新
詳細: [詳細]
\`\`\`
`,
  subagent_type: "general",
  tools: { "background_task": false }
})
```

---

## 🎯 完了待ち

```javascript
task({
  description: "タスク",
  prompt: "あなたはエージェントAです。\n\nタスク: [タスク]\n\n完了したら報告してください。",
  subagent_type: "general",
  tools: { "background_task": false }
})
```

---

## 💡 使い方

1. **上のコードをコピー**
2. **`[ここにタスクを書く]` の部分を書き換える**
3. **実行する**

これだけです！
