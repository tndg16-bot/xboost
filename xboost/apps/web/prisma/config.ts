/**
 * Prisma 7.x Configuration
 *
 * In Prisma 7.x, datasource URLs can be managed via this config file
 * instead of hardcoding them in schema.prisma
 */

export const datasourceUrl = process.env.DATABASE_URL;
